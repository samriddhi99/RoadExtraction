def process_query():
    # This function will interpret the natural language query and convert it to SQL
    # For simplicity, let's assume the query is always "SELECT * FROM locations WHERE name = 'some_name'"
    # In a real-world scenario, you would use an NLP library to parse the query
    pass